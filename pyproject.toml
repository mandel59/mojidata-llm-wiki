[project]
name = "mojidata-llm-wiki"
version = "0.1.0"
description = "Unicode standardization registry catalog and LLM-maintained wiki tooling"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pypdf>=6.14.2",
]
